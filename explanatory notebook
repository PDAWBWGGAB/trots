#I used the xgboost package from maching learning to fit a model that predicts the future outcomes of the races,
#where I chose a certian variables as predictors and the finishpostion as the dependent variable. 
